# OSS 库抽取实现计划

- [x] 1. 创建 OSS 库项目结构

  - 在 workspace 根目录创建 `oss-client` 子项目
  - 设置 `Cargo.toml` 配置文件，添加必要依赖
  - 创建基本的目录结构和模块文件
  - _需求: 1.1, 3.1_

- [x] 2. 实现配置管理模块

  - [x] 2.1 创建配置结构体和默认值

    - 实现 `OssConfig` 结构体，包含所有 OSS 配置字段
    - 设置合理的默认配置值（endpoint、bucket、region、upload_directory）
    - 实现配置验证逻辑，确保必要字段不为空
    - _需求: 3.1, 3.2, 3.5_

  - [x] 2.2 实现环境变量配置加载
    - 实现 `from_env()` 方法，从环境变量读取 access keys
    - 实现 `new()` 方法，支持直接传入 access keys
    - 添加配置验证和错误处理
    - _需求: 3.2, 3.5_

- [x] 3. 实现错误处理模块

  - [x] 3.1 定义错误类型枚举
    - 创建 `OssError` 枚举，包含配置、网络、文件、权限等错误类型
    - 使用 `thiserror` 实现错误信息格式化
    - 定义 `Result<T>` 类型别名
    - _需求: 4.1, 4.2, 4.3, 4.4, 4.5_

- [x] 4. 实现工具函数模块

  - [x] 4.1 实现 MIME 类型检测

    - 根据文件扩展名返回对应的 MIME 类型
    - 支持常见文件格式（图片、文档、音频、视频等）
    - 提供默认的 `application/octet-stream` 类型
    - _需求: 1.1_

  - [x] 4.2 实现文件名处理工具
    - 实现文件名清理函数，移除特殊字符
    - 实现唯一文件名生成，包含时间戳和 UUID
    - 实现文件大小格式化显示
    - _需求: 1.1_

- [x] 5. 实现 OSS 客户端核心功能

  - [x] 5.1 创建 OSS 客户端结构体

    - 实现 `OssClient` 结构体，封装 aliyun-oss-rust-sdk
    - 实现客户端初始化和配置验证
    - 添加连接测试功能
    - _需求: 1.1, 1.2, 3.1_

  - [x] 5.2 实现基本文件操作
    - 实现 `upload_file()` 方法，支持从本地路径上传文件
    - 实现 `upload_content()` 方法，支持直接上传字节数组
    - 实现 `download_file()` 方法，支持下载文件到本地路径
    - 实现 `delete_file()` 方法，支持删除 OSS 文件
    - 实现 `file_exists()` 方法，检查文件是否存在
    - _需求: 1.2, 1.3, 1.4, 1.5, 2.5_

- [x] 6. 实现签名 URL 功能

  - [x] 6.1 实现上传签名 URL 生成

    - 实现 `generate_upload_url()` 方法，生成带时效性的上传 URL
    - 支持自定义 Content-Type 参数
    - 支持自定义过期时间设置
    - _需求: 2.1, 2.4_

  - [x] 6.2 实现下载签名 URL 生成
    - 实现 `generate_download_url()` 方法，生成下载 URL
    - 支持永久有效和带时效性两种模式
    - 支持自定义过期时间设置
    - _需求: 2.2, 2.3_

- [x] 7. 实现库入口和公共 API

  - [x] 7.1 创建库入口文件

    - 在 `lib.rs` 中重新导出主要类型和函数
    - 实现便捷的客户端创建函数
    - 添加库文档注释和使用说明
    - _需求: 1.1_

  - [x] 7.2 实现便捷 API 函数
    - 实现 `create_client()` 便捷函数
    - 实现 `create_client_from_env()` 便捷函数
    - 确保 API 接口简洁易用
    - _需求: 1.1_

- [x] 8. 创建使用示例和文档

  - [x] 8.1 创建基本使用示例

    - 在 `examples/basic_usage.rs` 中创建基本文件操作示例
    - 展示客户端创建、文件上传下载等基本功能
    - 包含错误处理的最佳实践
    - _需求: 1.1_

  - [x] 8.2 创建签名 URL 使用示例
    - 在 `examples/signed_url.rs` 中创建签名 URL 示例
    - 展示上传和下载签名 URL 的生成和使用
    - 包含不同时效性设置的示例
    - _需求: 2.1, 2.2_

- [x] 9. 集成测试和验证

  - [x] 9.1 编写单元测试

    - 为配置模块编写测试用例
    - 为工具函数编写测试用例
    - 为错误处理编写测试用例
    - _需求: 3.5, 4.1_

  - [x] 9.2 编写集成测试
    - 创建 OSS 操作的集成测试（需要测试环境）
    - 测试签名 URL 的生成和使用
    - 验证错误处理的正确性
    - _需求: 1.1, 2.1, 2.2_

- [-] 10. 更新 workspace 配置和文档

  - [x] 10.1 更新 workspace Cargo.toml

    - 在根目录 `Cargo.toml` 中添加 `oss-client` 成员
    - 配置共享依赖版本管理
    - 确保 workspace 构建正常
    - _需求: 1.1_

  - [x] 10.2 创建 README 和文档
    - 为 `oss-client` 创建详细的 README.md
    - 包含安装、配置、使用说明
    - 提供完整的 API 文档
    - _需求: 1.1_

- [ ] 11. 重构现有项目使用新库

  - [x] 11.1 更新 document-parser 项目依赖

    - 在 `document-parser/Cargo.toml` 中添加 `oss-client` 依赖
    - 替换现有的 OSS 相关导入
    - 确保编译通过
    - _需求: 1.1_

  - [x] 11.2 重构 OSS 相关代码
    - 更新 `oss_handler.rs` 使用新的 OSS 库
    - 移除 `oss_service.rs` 中重复的功能
    - 保持现有 API 接口不变
    - 验证功能正常工作
    - _需求: 1.1_
